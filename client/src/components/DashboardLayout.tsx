import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { currencyCatalog, languageCatalog, useLocale } from "@/contexts/LocaleContext";
import { useIsMobile } from "@/hooks/useMobile";
import { BadgeDollarSign, FileText, Languages, LayoutDashboard, LogOut, Package, Settings, Users } from "lucide-react";
import React from "react";
import { useLocation } from "wouter";
import { Avatar, AvatarFallback } from "./ui/avatar";
import { Button } from "./ui/button";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "./ui/dropdown-menu";
import { Sidebar, SidebarContent, SidebarFooter, SidebarHeader, SidebarInset, SidebarMenu, SidebarMenuButton, SidebarMenuItem, SidebarProvider, SidebarTrigger } from "./ui/sidebar";
import { DashboardLayoutSkeleton } from "./DashboardLayoutSkeleton";

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  const { loading, user } = useAuth();
  if (loading) return <DashboardLayoutSkeleton />;
  if (!user) return <div className="grid min-h-screen place-items-center bg-slate-50 p-6"><div className="w-full max-w-md rounded-3xl border border-blue-100 bg-white p-8 text-center shadow-xl shadow-blue-100/50"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-blue-700 text-lg font-black text-white">IF</div><h1 className="mt-6 text-2xl font-bold tracking-tight text-slate-950">Welcome to invoice-gen.net</h1><p className="mt-3 text-sm leading-6 text-slate-500">Sign in to securely manage customers, invoices, and payment records.</p><Button className="mt-7 w-full bg-blue-700 hover:bg-blue-800" size="lg" onClick={startLogin}>Sign in to continue</Button></div></div>;
  return <SidebarProvider><DashboardContent>{children}</DashboardContent></SidebarProvider>;
}

function DashboardContent({ children }: { children: React.ReactNode }) {
  const { user, logout } = useAuth();
  const { t, locale, setLocale, toggleLocale, currency, setCurrency } = useLocale();
  const [location, navigate] = useLocation();
  const isMobile = useIsMobile();
  const menu = [{ path: "/app", label: t("dashboard"), icon: LayoutDashboard }, { path: "/invoices", label: t("invoices"), icon: FileText }, { path: "/customers", label: t("customers"), icon: Users }, { path: "/products", label: t("products"), icon: Package }, { path: "/settings", label: t("settings"), icon: Settings }];
  const active = menu.find((item) => item.path === location || (item.path !== "/" && location.startsWith(item.path)))?.label ?? "invoice-gen.net";

  return <><Sidebar collapsible="icon" className="border-r border-slate-200 bg-white"><SidebarHeader className="h-[76px] border-b border-slate-100 px-3"><button onClick={() => navigate("/app")} className="flex w-full items-center gap-3 rounded-xl px-2 text-left"><div className="grid h-9 w-9 place-items-center rounded-xl bg-blue-700 text-xs font-black text-white">IF</div><span className="font-bold tracking-tight text-slate-950 group-data-[collapsible=icon]:hidden">invoice-gen.net</span></button></SidebarHeader><SidebarContent className="py-4"><SidebarMenu className="px-3">{menu.map((item) => <SidebarMenuItem key={item.path}><SidebarMenuButton isActive={item.path === "/app" ? location === "/app" : location.startsWith(item.path)} tooltip={item.label} onClick={() => navigate(item.path)} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") { event.preventDefault(); navigate(item.path); } }} className="h-11 rounded-xl text-slate-600 data-[active=true]:bg-blue-50 data-[active=true]:font-semibold data-[active=true]:text-blue-800"><item.icon className="h-[18px] w-[18px]" /><span>{item.label}</span></SidebarMenuButton></SidebarMenuItem>)}</SidebarMenu></SidebarContent><SidebarFooter className="border-t border-slate-100 p-3"><div className="mb-2 flex h-10 w-full items-center gap-2 rounded-xl px-2 text-sm font-medium text-slate-600"><Languages className="h-4 w-4 shrink-0" /><select aria-label={t("language")} value={locale} onChange={(event) => setLocale(event.target.value as typeof locale)} className="min-w-0 flex-1 bg-transparent text-sm outline-none group-data-[collapsible=icon]:hidden">{languageCatalog.map((language) => <option key={language.code} value={language.code}>{language.nativeName}</option>)}</select><button aria-label={t("language")} onClick={toggleLocale} className="hidden group-data-[collapsible=icon]:block">{locale === "bn" ? "EN" : "বাং"}</button></div><div className="mb-3 flex h-10 w-full items-center gap-2 rounded-xl px-2 text-sm font-medium text-slate-600"><BadgeDollarSign className="h-4 w-4 shrink-0" /><select aria-label={t("currency")} value={currency} onChange={(event) => setCurrency(event.target.value as typeof currency)} className="min-w-0 flex-1 bg-transparent text-sm outline-none group-data-[collapsible=icon]:hidden">{currencyCatalog.map((entry) => <option key={entry.code} value={entry.code}>{entry.code} — {entry.nativeName}</option>)}</select></div><DropdownMenu><DropdownMenuTrigger asChild><button className="flex w-full items-center gap-3 rounded-xl p-1 text-left transition hover:bg-slate-50"><Avatar className="h-9 w-9 border border-blue-100"><AvatarFallback className="bg-blue-50 text-xs font-bold text-blue-700">{user?.name?.slice(0, 1).toUpperCase() ?? "U"}</AvatarFallback></Avatar><div className="min-w-0 group-data-[collapsible=icon]:hidden"><p className="truncate text-sm font-semibold text-slate-800">{user?.name || "Account"}</p><p className="truncate text-xs text-slate-500">{user?.email || ""}</p></div></button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem className="text-red-600 focus:text-red-600" onClick={logout}><LogOut className="mr-2 h-4 w-4" />Sign out</DropdownMenuItem></DropdownMenuContent></DropdownMenu></SidebarFooter></Sidebar><SidebarInset className="min-h-screen bg-[#f7faff]">{isMobile ? <header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b border-slate-200 bg-white/95 px-4 backdrop-blur"><div className="flex items-center gap-3"><SidebarTrigger /><span className="min-w-0 flex-1 truncate text-sm font-semibold text-slate-900"><span className="text-blue-700">invoice-gen.net</span><span className="mx-1 text-slate-300">/</span>{active}</span></div><button aria-label="Change language" onClick={toggleLocale} className="rounded-lg bg-blue-50 p-2 text-blue-700"><Languages className="h-4 w-4" /></button></header> : null}<main className="min-h-full p-4 sm:p-6 lg:p-8">{children}<footer className="mt-10 border-t border-slate-200 px-1 pt-6 text-xs text-slate-500 print:hidden"><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><span>© {new Date().getFullYear()} invoice-gen.net</span><span>{t("footerTagline")}</span></div></footer></main></SidebarInset></>;
}
